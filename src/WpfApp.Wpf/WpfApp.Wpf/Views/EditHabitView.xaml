<UserControl x:Class="WpfApp.Wpf.Views.EditHabitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Wpf.Views"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ui:Flyout Placement="BottomEdgeAlignedLeft">
        <StackPanel>
            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Desctiption"/>
            <TextBox Text=""/>
            <Button Content="Edytuj" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </ui:Flyout>
</UserControl>
