<Window x:Class="WpfApp.Wpf.Views.MainVindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Wpf.Views"
        mc:Ignorable="d"
        Title="MainVindowView" Height="450" Width="800"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        ui:WindowHelper.UseModernWindowStyle="True">
    <!--<StackPanel Orientation="Horizontal">
        <local:HomeView DataContext="{Binding HomeViewModel}"/>
        <local:CreateAtomicHabitView DataContext="{Binding CreateAtomicHabitViewModel}"/>
    </StackPanel>-->

    <Grid>
        <ui:NavigationView x:Name="NavView"
                           PaneDisplayMode="LeftCompact"
                           IsBackButtonVisible="Collapsed"
                           CompactModeThresholdWidth="48"
                           MenuItemsSource="{Binding MenuItems}"
                           SelectedItem="{Binding SelectedMenuItem, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                           >
            <ContentControl Content="{Binding CurrentView}"/>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding HamburgerMenuSelectionChangedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ui:NavigationView>
    </Grid>
</Window>
